{
  "name": "simple-test-app",
  "version": "1.0.0",
  "description": "A minimal web app demonstrating user authentication",
  "main": "server/index.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "node server/index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "bcrypt": "^5.0.1",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22",
    "jest": "^29.6.1"
  }
}